# Optimize your apartment search in Copenhagen

## Setup

In the project folder, run:

`conda env create -f environment.yml`

and then

`conda activate find_apartment`

## Scrape & Preprocess data

In the project folder, run:

`./run.sh`

## Frontend

In the project folder, run:

`streamlit run src/app.py`